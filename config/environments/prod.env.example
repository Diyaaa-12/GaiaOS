# Production environment — copy to repo root as .env when deploying to production.
# Replace placeholders with production values. Do not commit real secrets.

GAIAOS_ENV=prod
LOG_LEVEL=WARNING
DATABASE_URL=postgresql://<username>:<password>@<prod-host>:5432/<database>
