# Staging environment — copy to repo root as .env when deploying to staging.
# Replace placeholders with staging-specific values. Do not commit real secrets.

GAIAOS_ENV=staging
LOG_LEVEL=INFO
DATABASE_URL=postgresql://<username>:<password>@<staging-host>:5432/<database>
