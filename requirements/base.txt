# Runtime dependencies.
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
fastapi>=0.111,<1
uvicorn[standard]>=0.29,<1
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.29,<1
pgvector>=0.3,<1
# Database migrations (Milestone 6).
alembic>=1.13,<2
# Structured logging (Milestone 8).
structlog>=24.1,<26
# Redis client (Milestone 1)
redis>=5.0,<6
# Stateful agent orchestration (Milestone 2)
langgraph>=1.0.0,<2.0.0
# Model Context Protocol SDK (Milestone 4)
# Pinned to the 1.x series until the planned MCP 2.x migration.
mcp>=1.2.0,<2
# Authentication dependencies (Milestone 1)
PyJWT>=2.8.0,<3
argon2-cffi>=23.1.0,<24
email-validator>=2.0,<3
# Task Queue (Phase 3 Milestone 3 & 8)
rq>=1.16
rq-scheduler>=0.14.0
# PostGIS Geometry ORM (Phase 3 Milestone 7)
geoalchemy2>=0.20.0,<1
# Resilience layer — retry/backoff (Phase 6 Milestone 1).
# Pinned to the already-resolved transitive version; bump deliberately with lockfile regen.
tenacity==9.1.4
# S3-compatible Backup Storage (Phase 6 Milestone 6).
boto3>=1.34,<2



